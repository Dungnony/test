# 🎓 Hệ Thống Quản Lý Trung Tâm - Frontend

Frontend application được xây dựng với **React + TypeScript + Redux Toolkit + Tailwind CSS**.

## 🚀 Tech Stack

- **React 18** - UI Library
- **TypeScript** - Type-safe JavaScript
- **Redux Toolkit** - State Management
- **React Router v6** - Client-side routing
- **Axios** - HTTP Client
- **Tailwind CSS** - Utility-first CSS framework
- **Vite** - Fast build tool
- **date-fns** - Date utilities

## 📦 Cài đặt & Chạy

```bash
# Install dependencies
npm install

# Run development server
npm run dev

# Build for production
npm run build

# Preview production build
npm run preview
```

App sẽ chạy tại: **http://localhost:3000**

Backend API phải chạy tại: **http://localhost:8080**

## 🔐 Login Credentials (Test)

```
Admin:
username: admin@example.com
password: password123

Staff:
username: staff@example.com
password: password123
```

## 🎯 Tính năng

✅ **Quản lý học viên** - CRUD, tìm kiếm, xem lịch sử  
✅ **Quản lý khóa học** - CRUD với datetime picker  
✅ **Đăng ký học** - Enroll student vào course  
✅ **Cấp chứng chỉ** - Admin/Staff only, cập nhật trạng thái Đạt/Không đạt  
✅ **Dashboard** - Tổng quan hệ thống  
✅ **Thống kê** - Theo tỉnh, theo năm, theo trạng thái  
✅ **JWT Authentication** - Token-based với auto-refresh  
✅ **Role-based Access Control** - ADMIN/STAFF/USER

## 📁 Cấu trúc

```
src/
├── components/     # Reusable components (Button, Input, Modal, Table...)
├── pages/          # Page components (Login, Dashboard, Students...)
├── redux/          # Redux store & slices
├── services/       # API services & Axios config
├── types/          # TypeScript interfaces
├── utils/          # Utilities (date, role, helpers)
├── App.tsx         # Main app with routing
└── main.tsx        # Entry point
```

## 🌐 API Endpoints

Base: `http://localhost:8080/api`

- **Auth**: `POST /auth/login`
- **Students**: `GET|POST|PUT|DELETE /v1/students`
- **Courses**: `GET|POST|PUT|DELETE /v1/courses`
- **Enrollments**: `POST /v1/enrollments`, `PUT /v1/enrollments/:id/certificate`
- **Statistics**: `GET /v1/stats/*`

## 🛡️ Roles & Permissions

| Feature                     | ADMIN | STAFF | USER |
| --------------------------- | ----- | ----- | ---- |
| View Students/Courses       | ✅    | ✅    | ✅   |
| Create/Edit/Delete Students | ✅    | ❌    | ❌   |
| Create/Edit/Delete Courses  | ✅    | ❌    | ❌   |
| Enroll Students             | ✅    | ✅    | ✅   |
| Issue Certificates          | ✅    | ✅    | ❌   |

## 📝 Notes

- **Date Format**: `YYYY-MM-DD` (ISO date)
- **DateTime Format**: `YYYY-MM-DDTHH:mm:ss` (ISO datetime)
- **MSV**: Auto-generated by backend (`HV00001`, `HV00002`...)
- **Token Expiry**: 10 hours
- **CORS**: Backend allows `http://localhost:3000`

## 🐛 Troubleshooting

1. **CORS Error** → Check backend CORS config
2. **401 Unauthorized** → Clear localStorage & login again
3. **404 Not Found** → Ensure backend running on port 8080

## 📚 Documentation

Xem chi tiết trong code comments và TypeScript types!
